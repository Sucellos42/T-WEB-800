version: '3.8'
services:
  nestjs:
    build:
      context: ./epicroad-api
      dockerfile: Dockerfile.dev
    volumes:
      - ./epicroad-api:/app # Montez le code source localement pour le développement en direct
    ports:
      - "3001:3000"
    environment:
      NODE_ENV: development
#  vuejs:
#    build: ./path-to-vuejs
#    volumes:
#      - ./path-to-vuejs:/app # Montez le code source localement pour le développement en direct
#    ports:
#      - "8080:8080"
#    environment:
#      NODE_ENV: development
#  db:
#    image: postgres
#    volumes:
#      - postgres-data:/var/lib/postgresql/data # Persiste les données entre les démarrages de conteneur
#    environment:
#      POSTGRES_DB: exampledb
#      POSTGRES_USER: exampleuser
#      POSTGRES_PASSWORD: examplepass
#
#volumes:
#  postgres-data:
