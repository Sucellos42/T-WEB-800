#!/bin/sh
. "$(dirname "$0")/_/husky.sh"
#npx lint-staged

# Client side
cd frontend && npm run lint && npm run format
tsc --noEmit

# Server side
cd ../backend-api && npm run lint && npm run format
tsc --noEmit

